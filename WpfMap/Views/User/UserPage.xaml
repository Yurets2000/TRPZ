<Window x:Class="WpfMap.UserPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMap"
        mc:Ignorable="d"
        Title="UserPage" Height="480" Width="640">
    <Grid x:Name="LayoutRoot">
        <DockPanel LastChildFill="False" Background="Bisque">
            <Grid Width="250" DockPanel.Dock="Left">
                <Grid.RowDefinitions>
                    <RowDefinition Height="75" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250" />
                </Grid.ColumnDefinitions>
                <Label Content="Travel Maps" Grid.Row="0" Grid.Column="0" Margin="10, 5" FontWeight="Bold" FontSize="32" FontFamily="Rockwell" HorizontalAlignment="Center" Foreground="Green"/>
                <Label Content="Select Country" Grid.Row="1" Grid.Column="0" Margin="10, 5" Style="{StaticResource BoldLabelStyle}"/>
                <TextBox Name="countryName" Text="Write Country Name" Grid.Row="2" Grid.Column="0" Margin="10, 5" Template="{StaticResource RoundedTextBoxTemplate}" Style="{StaticResource DefaultTextBoxStyle}"/>
                <Button Click="SearchByName_Click" Content="Search" Grid.Row="3" Grid.Column="0" Margin="10, 5" Template="{StaticResource RoundedButtonTemplate}" Style="{StaticResource DefaultButtonStyle}"/>
                <Image Source="D:/profiles/map.png" Grid.Row="4" Grid.Column="0" Height="200" Margin="15,50"/>
            </Grid>
            <StackPanel>
                <ListView x:Name="countries" Width="390" VerticalAlignment="Top" DockPanel.Dock="Right" Height="350">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Country" Width="390" DisplayMemberBinding="{Binding Path=Name}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                <Button Click="FindCountry_Click" Content="Select" Grid.Row="3" Grid.Column="0" Margin="20" Template="{StaticResource RoundedButtonTemplate}" Style="{StaticResource DefaultButtonStyle}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
